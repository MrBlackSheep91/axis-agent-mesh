[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "axis-agent-def"
version = "0.1.0"
description = "Pydantic v2 mirror of the canonical AgentDefinition contract for the axis ecosystem (kept in sync with the TypeScript interface and JSON Schema)."
readme = "README.md"
requires-python = ">=3.9"
license = {text = "Proprietary"}
authors = [
    {name = "Maicol Martinez"}
]
dependencies = [
    "pydantic>=2",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.setuptools.packages.find]
where = ["python"]

[tool.setuptools.package-dir]
"" = "python"
